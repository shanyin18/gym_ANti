# Gym ANti 项目启动指南

> 适用当前仓库版本（后端单轨：`server-python/main.py`，向量库为 Milvus）

## 1. 端口与服务总览

- 前端（Vite）：`5176`
- 后端（FastAPI）：`8000`
- PostgreSQL：`5432`
- Redis：`6379`
- Milvus：`19530`（服务）/ `9091`（管理）
- MinIO：`9000`（API）/ `9001`（Console）
- Prometheus（可选）：`9090`
- Grafana（可选）：`3001`

---

## 2. 推荐方式：Docker 一键启动

### 2.1 前置要求

- Docker Desktop 已安装并运行
- Windows / macOS / Linux 均可

### 2.2 准备后端环境变量

首次启动前，确保存在 `server-python/.env`。

如果没有，先复制：

```powershell
Copy-Item "server-python/.env.example" "server-python/.env"
```

然后手动修改 `server-python/.env` 里至少这些项（示例）：

```env
DB_USER=postgres
DB_PASSWORD=130131
DB_NAME=littlfish
DB_PORT=5432

JWT_SECRET=replace_with_your_secret
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=1440

DOUBAO_API_KEY=replace_with_your_key
DOUBAO_BASE_URL=https://ark.cn-beijing.volces.com/api/v3
DOUBAO_MODEL=ep-20251223195501-9x6g5
DOUBAO_EMBEDDING_MODEL=replace_with_your_embedding_model

COHERE_API_KEY=
UNSTRUCTURED_API_KEY=
```

> 说明：`docker-compose.yml` 会为容器内后端覆盖 `DB_HOST=postgres`、`MILVUS_HOST=standalone`、`REDIS_HOST=redis` 等关键项。

### 2.3 启动主业务栈

```powershell
docker compose up -d --build
```

### 2.4 检查是否启动成功

```powershell
docker compose ps
```

后端健康检查：

```powershell
curl.exe -sS http://127.0.0.1:8000/health
```

预期示例：

```json
{"status":"ok","timestamp":"...","database":"connected"}
```

前端访问：

- `http://127.0.0.1:5176`

### 2.5 停止/重启

停止：

```powershell
docker compose down
```

仅重启后端：

```powershell
docker compose restart backend
```

---

## 3. 可选：启动监控栈（Prometheus + Grafana）

```powershell
docker compose -f docker-compose.monitoring.yml up -d
```

访问：

- Prometheus: `http://127.0.0.1:9090`
- Grafana: `http://127.0.0.1:3001`
  - 默认账号：`admin`
  - 默认密码：`admin`

停止监控栈：

```powershell
docker compose -f docker-compose.monitoring.yml down
```

---

## 4. 本地开发方式（后端/前端分开跑）

## 4.1 先拉起基础依赖（DB/Redis/Milvus）

```powershell
docker compose up -d etcd minio standalone postgres redis
```

## 4.2 启动后端（本机 Python）

```powershell
cd server-python
pip install -r requirements.txt
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

本地运行时，`server-python/.env` 推荐：

```env
DB_HOST=localhost
DB_PORT=5432
REDIS_HOST=localhost
REDIS_PORT=6379
MILVUS_HOST=localhost
MILVUS_PORT=19530
```

## 4.3 启动前端（本机 Node）

```powershell
npm install
npm run dev
```

---

## 5. 常见问题排查

## 5.1 `curl /health` 返回空或失败

- 先看状态：

```powershell
docker compose ps backend
docker compose logs --tail 100 backend
```

- 若刚启动，等待 10-30 秒再试（首次冷启动可能有预热）

## 5.2 PostgreSQL 连接失败（常见端口写错）

- 确保是 `5432`，不是历史值 `1211`
- 容器内后端应连 `postgres:5432`
- 本地后端应连 `localhost:5432`

## 5.3 Milvus `proxy not healthy`

- 依赖服务未完全就绪，执行：

```powershell
docker compose restart standalone backend
```

## 5.4 挂载报错（`volumes` 路径异常）

如果提示 `.../volumes/...` 挂载失败，手动创建目录后重试：

```powershell
New-Item -ItemType Directory -Path ".\volumes\etcd" -Force | Out-Null
New-Item -ItemType Directory -Path ".\volumes\minio" -Force | Out-Null
New-Item -ItemType Directory -Path ".\volumes\milvus" -Force | Out-Null
New-Item -ItemType Directory -Path ".\volumes\postgres" -Force | Out-Null
New-Item -ItemType Directory -Path ".\volumes\redis" -Force | Out-Null
docker compose up -d
```

## 5.5 想彻底清空数据重来

```powershell
docker compose down
Remove-Item -Recurse -Force ".\volumes" -ErrorAction SilentlyContinue
docker compose up -d --build
```

---

## 6. 推荐启动顺序（最稳）

1. 配置 `server-python/.env`
2. `docker compose up -d --build`
3. `docker compose ps`
4. `curl /health`
5. 打开前端 `http://127.0.0.1:5176`

