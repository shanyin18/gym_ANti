# FastAPI + LangChain + RAG Backend
# 基于 fastapi_env 环境已有的库

# ===== 已在环境内 (无需安装) =====
fastapi==0.128.0
uvicorn==0.40.0
langchain>=0.2.0,<1.0.0
langchain-community>=0.2.0,<1.0.0
langchain-openai>=0.1.0,<1.0.0
langchain-chroma>=0.1.0     # Chroma Vector Store
pymilvus>=2.4.0         # Milvus Python SDK
sqlalchemy==2.0.45
python-dotenv==1.2.1
bcrypt==5.0.0
httpx==0.28.1
pydantic==2.12.5
redis==5.0.1  # Redis Client

# ===== 仍需安装 (补课) =====
python-jose[cryptography] # JWT 解析
passlib[bcrypt]       # 密码哈希
psycopg2-binary       # PostgreSQL 驱动 (同步)
asyncpg               # PostgreSQL 异步驱动
greenlet              # SQLAlchemy async 所需
rank_bm25             # BM25 检索算法
langchain-cohere      # Cohere Rerank
ragas                 # RAG 评估框架
datasets              # HuggingFace datasets (RAGAs 依赖)
unstructured-client   # Unstructured API 客户端 (PDF解析)
tenacity              # 重试机制

# ===== 链路追踪 (第五周新增) =====
langsmith             # LangSmith 链路追踪

# ===== 指标监控 (Prometheus) =====
prometheus-fastapi-instrumentator>=7.0.0

# ===== 性能分析工具 (新增) =====
py-spy                # 采样分析器
locust                # 负载测试工具
snakeviz              # cProfile 可视化
